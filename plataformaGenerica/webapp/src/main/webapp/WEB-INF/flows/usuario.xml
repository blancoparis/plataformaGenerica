<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow
                          http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd"
      start-state="setupForm">

	<action-state id="setupForm" >
		<evaluate expression="formAction.setupForm" />
		<transition on="success" to="welcome"  /> 
	</action-state>

    <view-state id="welcome" view="pruebaFormulario" model="form" >
        <transition on="submit" to="bookingCancelled" validate="true" bind="true" >
<!--         	<evaluate expression="formAction.bind"/> -->
        </transition>
        <transition on="establecerModificacion" to="establecerModificacion" bind="true"/>
    </view-state>
	<action-state id="establecerModificacion">
		<evaluate expression="formAction.establecerModificacion"/>
		<transition on="success" to="welcome"/>
	</action-state>
    <action-state id="bookingCancelled" >
    	<evaluate expression="formAction.confirmar"/>
    	<transition on="success" to="resultado"/>
    	<transition on="error" to="welcome"/>
    </action-state>
    <end-state id="resultado" view="resultado"/>
	<bean-import resource="usuario-bean.xml"/>	
</flow>